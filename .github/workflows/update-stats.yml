name: Update Douyin Stats

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  update-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.PAT }}
        
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Update Douyin statistics
      run: |
        echo "开始执行抖音数据更新..."
        python scripts/simple_test.py  # 这里修改了！
        echo "数据更新完成"
        
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/stats.json
        
        if git diff --staged --quiet; then
          echo "没有数据变化，跳过提交"
        else
          git commit -m "🤖 Auto-update: 抖音粉丝数据更新"
          git push
          echo "数据已提交并推送"
        fi
